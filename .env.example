# Server-side only. Set these in Vercel Project Settings → Environment Variables.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Public worker URL, with no credential embedded. Used to activate custom agents.
VITE_AGENT_RUNTIME_URL=http://127.0.0.1:8000
